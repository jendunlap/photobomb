const Grid = (props) => {
  return (
    <div className="gridDiv">
      <div className="infoWrapper">
        <h1 className="gridName">{props.name}</h1>
      </div>
    </div>
  )
}

export default Grid
